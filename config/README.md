Configuration

Preferred approach: keep secrets out of git by creating a top-level .env file (see .env.example) and using python-dotenv or your deployment's secret manager.
Use config/example_config.yml if you prefer YAML-based configuration.
